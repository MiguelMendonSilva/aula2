#include <stdio.h>

int main()
{
    float usd = 5.250;
    float euro = 5.750;
    float btc = 420000.000;

    printf("Cotação do Dólar (USD): %.3f BRL\n", usd);
    printf("Cotação do Euro (EUR): %.3f BRL\n", euro);
    printf("Cotação do Bitcoin (BTC): %.3f BRL\n", btc);

    return 0;
}